import { styled } from 'styled-components';

export const SenderFormWrapper = styled.div`
  min-height: 560px;
  position: relative;
  padding: 40px;
`;

export const Title = styled.h1`
  font-size: 30px;
  font-weight: 500;
  color: var(--gray-iron-900);
  line-height: 38px;
  margin-bottom: 32px;
`;
